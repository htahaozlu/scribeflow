# Idempotent PyPI publish via OIDC trusted publishing. Hardened in P9.
name: publish

on:
  release:
    types: [published]

permissions:
  id-token: write  # OIDC trusted publishing — no API token needed

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true  # tolerate "already uploaded" (idempotent)
